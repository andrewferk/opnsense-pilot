[project]
name = "pilot-transport-probe"
version = "0"
description = "THROWAWAY: Streamable HTTP MCP transport test against two real clients (wayfinder ticket #14)"
requires-python = ">=3.13,<3.14"
dependencies = [
    "mcp==2.2.0",
    "fastapi",
    "uvicorn",
    "pydantic>=2.12",
]

[dependency-groups]
dev = ["pytest", "anyio"]
